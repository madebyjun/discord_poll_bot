name: Create Daily Discord Poll

on:
  schedule:
    - cron: '0 0 * * *'  # This is UTC time, adjust accordingly to match 9 AM in your local time
  workflow_dispatch:  # これを追加

jobs:
  create_poll:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3  # バージョンをv3に更新

    - name: Set up Python
      uses: actions/setup-python@v4  # バージョンをv4に更新
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run create_poll.py
      run: python create_poll.py
      env:
        DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
        DISCORD_GUILD_ID: ${{ secrets.DISCORD_GUILD_ID }}
        DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
